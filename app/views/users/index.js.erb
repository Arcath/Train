<% unless @autocompletes %>
	$("#pagination").html("<%= escape_javascript(render("users")) %>");
<% else %>
	$("#auto_complete_results").html("<%= escape_javascript(render("results")) %>");
<% end %>
